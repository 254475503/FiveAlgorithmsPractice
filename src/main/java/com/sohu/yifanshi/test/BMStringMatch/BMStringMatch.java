package com.sohu.yifanshi.test.BMStringMatch;

import java.util.HashSet;
import java.util.stream.Collectors;

public class BMStringMatch {//todo 端午结束之前，实现一个bm算法

    public static void main(String[] args) {
        HashSet<Integer> set = new HashSet<>();
        set.stream().mapToInt(Integer::valueOf).toArray();
    }
}
